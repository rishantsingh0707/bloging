<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('./partials/head') %>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        overflow-x: hidden;
      }

      .main-content {
        position: relative;
        min-height: 100vh;
        padding-top: 80px;
      }

      .floating-bg {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        z-index: 1;
      }

      .circle {
        position: absolute;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        animation: float 6s ease-in-out infinite;
      }

      .circle1 {
        width: 80px;
        height: 80px;
        top: 10%;
        left: 10%;
        animation-delay: 0s;
      }

      .circle2 {
        width: 120px;
        height: 120px;
        top: 20%;
        right: 10%;
        animation-delay: 1s;
      }

      .circle3 {
        width: 60px;
        height: 60px;
        top: 60%;
        left: 20%;
        animation-delay: 2s;
      }

      .circle4 {
        width: 100px;
        height: 100px;
        bottom: 20%;
        right: 20%;
        animation-delay: 3s;
      }

      .circle5 {
        width: 40px;
        height: 40px;
        top: 40%;
        left: 60%;
        animation-delay: 4s;
      }

      .circle6 {
        width: 90px;
        height: 90px;
        top: 70%;
        right: 40%;
        animation-delay: 5s;
      }

      .circle7 {
        width: 70px;
        height: 70px;
        bottom: 40%;
        left: 40%;
        animation-delay: 1.5s;
      }

      .circle8 {
        width: 110px;
        height: 110px;
        top: 30%;
        right: 60%;
        animation-delay: 2.5s;
      }

      .floating-orb {
        position: absolute;
        border-radius: 50%;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, rgba(255, 255, 255, 0.1) 70%);
        backdrop-filter: blur(15px);
        animation: orbit 8s linear infinite;
      }

      .orb1 {
        width: 50px;
        height: 50px;
        top: 25%;
        left: 80%;
        animation-delay: 0s;
      }

      .orb2 {
        width: 30px;
        height: 30px;
        top: 80%;
        left: 15%;
        animation-delay: 3s;
      }

      .orb3 {
        width: 40px;
        height: 40px;
        top: 50%;
        right: 10%;
        animation-delay: 6s;
      }

      .glass-shard {
        position: absolute;
        width: 2px;
        height: 60px;
        background: linear-gradient(45deg, rgba(255, 255, 255, 0.6), rgba(255, 255, 255, 0.1));
        transform: rotate(45deg);
        animation: shimmer 4s ease-in-out infinite;
      }

      .shard1 {
        top: 15%;
        left: 30%;
        animation-delay: 1s;
      }

      .shard2 {
        bottom: 30%;
        right: 25%;
        animation-delay: 3s;
      }

      .hero-section {
        position: relative;
        z-index: 2;
        padding: 60px 0 40px;
        text-align: center;
      }

      .hero-content {
        max-width: 600px;
        margin: 0 auto;
      }

      .main-heading {
        font-size: 3.5rem;
        font-weight: 700;
        background: linear-gradient(135deg, #ffffff 0%, #f8f9ff 50%, #e8eaff 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 1rem;
        text-shadow: 0 4px 20px rgba(255, 255, 255, 0.3);
        animation: textShimmer 3s ease-in-out infinite;
      }

      .sub-heading {
        font-size: 1.5rem;
        font-weight: 400;
        color: rgba(255, 255, 255, 0.9);
        margin-bottom: 1rem;
        letter-spacing: 0.5px;
      }

      .animated-subtext {
        font-size: 1.1rem;
        color: rgba(255, 255, 255, 0.8);
        margin-bottom: 2rem;
        animation: fadeInUp 1s ease-out;
      }

      .decorative-line {
        width: 100px;
        height: 2px;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.6), transparent);
        margin: 0 auto;
        animation: pulse 2s ease-in-out infinite;
      }

      .signup-section {
        position: relative;
        z-index: 2;
        padding: 40px 0 80px;
      }

      .form-wrapper {
        max-width: 500px;
        margin: 0 auto;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 20px;
        padding: 40px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        position: relative;
        overflow: hidden;
      }

      .form-wrapper::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 1px;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.6), transparent);
        animation: shimmerLine 3s ease-in-out infinite;
      }

      .form-header {
        text-align: center;
        margin-bottom: 30px;
      }

      .form-title {
        font-size: 1.8rem;
        font-weight: 600;
        color: white;
        margin-bottom: 0.5rem;
      }

      .form-subtitle {
        color: rgba(255, 255, 255, 0.7);
        font-size: 0.95rem;
      }

      .profile-section {
        text-align: center;
        margin-bottom: 30px;
      }

      .profile-heading {
        font-size: 1.2rem;
        color: white;
        margin-bottom: 15px;
        font-weight: 500;
      }

      .profile-image-container {
        position: relative;
        display: inline-block;
        margin-bottom: 20px;
      }

      .profile-image {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        border: 3px solid rgba(255, 255, 255, 0.3);
        transition: all 0.3s ease;
        cursor: pointer;
      }

      .profile-image:hover {
        border-color: rgba(255, 255, 255, 0.6);
        transform: scale(1.05);
      }

      .image-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.5);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        transition: opacity 0.3s ease;
        cursor: pointer;
      }

      .profile-image-container:hover .image-overlay {
        opacity: 1;
      }

      .upload-text {
        color: white;
        font-size: 0.8rem;
        font-weight: 500;
      }

      .form-fields {
        margin-bottom: 30px;
      }

      .field-group {
        margin-bottom: 25px;
      }

      .field-group-title {
        font-size: 1.1rem;
        color: white;
        margin-bottom: 15px;
        font-weight: 500;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        padding-bottom: 8px;
      }

      .input-wrapper {
        position: relative;
        margin-bottom: 20px;
      }

      .form-label {
        display: block;
        color: rgba(255, 255, 255, 0.9);
        font-size: 0.9rem;
        font-weight: 500;
        margin-bottom: 8px;
      }

      .form-control {
        width: 100%;
        padding: 12px 16px;
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 10px;
        color: white;
        font-size: 1rem;
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
      }

      .form-control::placeholder {
        color: rgba(255, 255, 255, 0.5);
      }

      .form-control:focus {
        outline: none;
        border-color: rgba(255, 255, 255, 0.5);
        background: rgba(255, 255, 255, 0.15);
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      }

      .input-decoration {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background: linear-gradient(90deg, #667eea, #764ba2);
        transition: width 0.3s ease;
      }

      .form-control:focus+.input-decoration {
        width: 100%;
      }

      .file-input {
        padding: 8px 12px;
        cursor: pointer;
      }

      .file-input-decoration {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background: linear-gradient(90deg, #667eea, #764ba2);
        transition: width 0.3s ease;
      }

      .file-input:focus+.file-input-decoration {
        width: 100%;
      }

      .submit-section {
        text-align: center;
      }

      .btn-submit {
        position: relative;
        width: 100%;
        padding: 15px 30px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        border-radius: 12px;
        color: white;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        overflow: hidden;
        margin-bottom: 20px;
      }

      .btn-submit:hover {
        transform: translateY(-3px);
        box-shadow: 0 15px 30px rgba(102, 126, 234, 0.4);
      }

      .btn-text {
        position: relative;
        z-index: 2;
      }

      .btn-shimmer {
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
        transition: left 0.6s ease;
      }

      .btn-submit:hover .btn-shimmer {
        left: 100%;
      }

      .signup-footer {
        color: rgba(255, 255, 255, 0.7);
        font-size: 0.9rem;
      }

      .login-link {
        color: white;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease;
      }

      .login-link:hover {
        color: #667eea;
      }

      @keyframes float {

        0%,
        100% {
          transform: translateY(0px) rotate(0deg);
        }

        50% {
          transform: translateY(-20px) rotate(180deg);
        }
      }

      @keyframes orbit {
        0% {
          transform: rotate(0deg) translateX(30px) rotate(0deg);
        }

        100% {
          transform: rotate(360deg) translateX(30px) rotate(-360deg);
        }
      }

      @keyframes shimmer {

        0%,
        100% {
          opacity: 0.3;
          transform: rotate(45deg) scale(1);
        }

        50% {
          opacity: 1;
          transform: rotate(45deg) scale(1.2);
        }
      }

      @keyframes textShimmer {

        0%,
        100% {
          background-position: 0% 50%;
        }

        50% {
          background-position: 100% 50%;
        }
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }

        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes pulse {

        0%,
        100% {
          opacity: 0.5;
          transform: scaleX(1);
        }

        50% {
          opacity: 1;
          transform: scaleX(1.2);
        }
      }

      @keyframes shimmerLine {

        0%,
        100% {
          transform: translateX(-100%);
        }

        50% {
          transform: translateX(100%);
        }
      }

      @media (max-width: 768px) {
        .main-heading {
          font-size: 2.5rem;
        }

        .sub-heading {
          font-size: 1.2rem;
        }

        .form-wrapper {
          margin: 0 20px;
          padding: 30px 20px;
        }

        .hero-section {
          padding: 40px 0 30px;
        }
      }

      @media (max-width: 480px) {
        .main-heading {
          font-size: 2rem;
        }

        .form-wrapper {
          margin: 0 15px;
          padding: 25px 15px;
        }
      }
    </style>
    <title>Sign Up</title>
</head>

<body>
  <%- include('./partials/nav') %>

    <div class="main-content">
      <div class="floating-bg">
        <div class="circle circle1"></div>
        <div class="circle circle2"></div>
        <div class="circle circle3"></div>
        <div class="circle circle4"></div>
        <div class="circle circle5"></div>
        <div class="circle circle6"></div>
        <div class="circle circle7"></div>
        <div class="circle circle8"></div>
        <div class="floating-orb orb1"></div>
        <div class="floating-orb orb2"></div>
        <div class="floating-orb orb3"></div>
        <div class="glass-shard shard1"></div>
        <div class="glass-shard shard2"></div>
      </div>

      <section class="hero-section">
        <div class="container text-center">
          <div class="hero-content">
            <h1 class="main-heading">Create Your Account</h1>
            <h2 class="sub-heading">Join Our Blogging Community</h2>
            <p class="animated-subtext">Let's get you blogging in style 💫</p>
            <div class="decorative-line"></div>
          </div>
        </div>
      </section>

      <section class="signup-section">
        <div class="container">
          <div class="form-wrapper">
            <div class="form-header">
              <h3 class="form-title">Personal Information</h3>
              <p class="form-subtitle">Tell us a bit about yourself</p>
            </div>

            <form action="/user/signup" method="post" enctype="multipart/form-data" class="signup-form">
              <div class="profile-section">
                <h4 class="profile-heading">Profile Picture</h4>
                <div class="profile-image-container">
                  <% if (user) { %>
                    <img src="<%= user.profileImageURL %>" alt="Profile Image" class="profile-image" />
                    <% } else { %>
                      <img src="/images/Default_Profile_Image.png" alt="Default Image" class="profile-image" />
                      <% } %>
                        <div class="image-overlay">
                          <span class="upload-text">Click to upload</span>
                        </div>
                </div>
              </div>

              <div class="form-fields">
                <div class="field-group">
                  <h4 class="field-group-title">Account Details</h4>

                  <div class="input-wrapper">
                    <label for="nameInput" class="form-label">Full Name</label>
                    <input type="text" class="form-control" id="nameInput" name="fullname"
                      placeholder="Enter your full name">
                    <div class="input-decoration"></div>
                  </div>

                  <div class="input-wrapper">
                    <label for="emailInput" class="form-label">Email Address</label>
                    <input type="email" class="form-control" id="emailInput" name="email"
                      placeholder="Enter your email">
                    <div class="input-decoration"></div>
                  </div>

                  <div class="input-wrapper">
                    <label for="passwordInput" class="form-label">Password</label>
                    <input type="password" class="form-control" id="passwordInput" name="password"
                      placeholder="Create a strong password">
                    <div class="input-decoration"></div>
                  </div>
                </div>

                <div class="field-group">
                  <h4 class="field-group-title">Profile Image</h4>
                  <div class="input-wrapper">
                    <label for="profileImage" class="form-label">Upload Profile Image</label>
                    <input type="file" class="form-control file-input" id="profileImage" name="profileImage"
                      accept="image/*" />
                    <div class="file-input-decoration"></div>
                  </div>
                </div>
              </div>

              <div class="submit-section">
                <button type="submit" class="btn-submit">
                  <span class="btn-text">Create Account</span>
                  <div class="btn-shimmer"></div>
                </button>
                <p class="signup-footer">Already have an account? <a href="/user/signin" class="login-link">Sign in here</a>
                </p>
              </div>
            </form>
          </div>
        </div>
      </section>
    </div>

    <%- include('./partials/script') %>
</body>

</html>